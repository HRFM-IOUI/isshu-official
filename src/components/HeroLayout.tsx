import HeroSection from "@/components/HeroSection";

export default function HeroLayout() {
  return (
    <section className="relative w-full overflow-hidden min-h-[520px] flex items-center justify-center bg-gradient-to-r from-[#2273d9] via-[#50a7f9] to-[#1861bf]">
      <HeroSection />
    </section>
  );
}
